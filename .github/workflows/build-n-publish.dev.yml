# This workflow builds and uploads python artifacts

name: Build and Publish Development Package
on:
  push:
    branches:
      - master

jobs:
  build-n-publish-package:
    permissions:
      contents: write
      id-token: write
      actions: write
    uses: irvingleonard/reusable-workflows/.github/workflows/python-build-publish-development-package.yml@v0.0.10
    with:
      package-name: ${{ vars.PACKAGE_NAME }}
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
      pypi_token: ${{ secrets.PYPI_TOKEN }}
